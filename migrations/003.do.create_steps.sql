CREATE TABLE steps (
  steps_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  bug_id INTEGER REFERENCES bugs(bug_id) ON DELETE CASCADE NOT NULL,
  steps_number INTEGER NULL,
  step TEXT NULL
);